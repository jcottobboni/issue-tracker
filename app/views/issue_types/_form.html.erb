<%= form_with(model: issue_type, local: true) do |form| %>
  <% if issue_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(issue_type.errors.count, "error") %> prohibited this issue_type from being saved:</h2>

      <ul>
      <% issue_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :string %>
    <%= form.text_field :string, id: :issue_type_string %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
